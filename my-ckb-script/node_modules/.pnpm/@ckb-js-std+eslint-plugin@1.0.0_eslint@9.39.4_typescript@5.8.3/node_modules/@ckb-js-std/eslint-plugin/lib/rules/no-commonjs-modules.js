"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.rule = void 0;
const utils_1 = require("@typescript-eslint/utils");
const utils_2 = require("../utils");
exports.rule = (0, utils_2.createRule)({
    name: "no-commonjs-modules",
    meta: {
        type: "problem",
        docs: {
            description: "Disallow CommonJS module patterns (`require`, `module.exports`, `exports.xxx`) as ckb-js-vm exclusively supports ECMAScript Modules (ESM). This version flags all occurrences without checking for shadowing.",
            recommended: true,
        },
        messages: {
            noRequire: "Use ES6 imports instead of `require()`. ckb-js-vm supports ESM only.",
            noModuleExports: "Use ES6 exports instead of `module.exports` or `exports = ...`. ckb-js-vm supports ESM only.",
            noExportsAssignment: "Use ES6 named exports instead of `exports.xxx`. ckb-js-vm supports ESM only.",
        },
        schema: [],
    },
    defaultOptions: [],
    create(context) {
        return {
            // require(...)
            CallExpression(node) {
                if (node.callee.type === utils_1.AST_NODE_TYPES.Identifier &&
                    node.callee.name === "require") {
                    context.report({ node, messageId: "noRequire" });
                }
            },
            AssignmentExpression(node) {
                const { left } = node;
                if (left.type === utils_1.AST_NODE_TYPES.MemberExpression) {
                    // module.exports = ...
                    if (left.object.type === utils_1.AST_NODE_TYPES.Identifier &&
                        left.object.name === "module" &&
                        left.property.type === utils_1.AST_NODE_TYPES.Identifier &&
                        left.property.name === "exports") {
                        context.report({ node: left, messageId: "noModuleExports" });
                    }
                    // exports.foo = ... OR module.exports.foo = ...
                    else if ((left.object.type === utils_1.AST_NODE_TYPES.Identifier &&
                        left.object.name === "exports") ||
                        (left.object.type === utils_1.AST_NODE_TYPES.MemberExpression &&
                            left.object.object.type === utils_1.AST_NODE_TYPES.Identifier &&
                            left.object.object.name === "module" &&
                            left.object.property.type === utils_1.AST_NODE_TYPES.Identifier &&
                            left.object.property.name === "exports")) {
                        context.report({ node: left, messageId: "noExportsAssignment" });
                    }
                }
                // exports = ...
                else if (left.type === utils_1.AST_NODE_TYPES.Identifier &&
                    left.name === "exports") {
                    context.report({ node: left, messageId: "noModuleExports" });
                }
            },
        };
    },
});
//# sourceMappingURL=no-commonjs-modules.js.map