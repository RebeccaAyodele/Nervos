"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.rule = void 0;
const utils_1 = require("@typescript-eslint/utils");
const utils_2 = require("../utils");
exports.rule = (0, utils_2.createRule)({
    name: "enforce-bindings-exit-main",
    meta: {
        type: "problem",
        docs: {
            description: "Enforce that `index.ts` (or `index.js`) exits via a top-level `bindings.exit(main());` call.",
            recommended: true,
        },
        messages: {
            missingBindingsExitMain: "The script must exit with a top-level call to `bindings.exit(main());`.",
            notTopLevel: "The `bindings.exit(main());` call must be at the top level of the module.",
        },
        schema: [],
    },
    defaultOptions: [],
    create(context) {
        const filename = context.filename;
        if (!/index\.(ts|js)$/i.test(filename)) {
            return {};
        }
        let foundTopLevel = false;
        let hasReported = false;
        return {
            CallExpression(node) {
                if (node.callee.type === utils_1.AST_NODE_TYPES.MemberExpression &&
                    node.callee.object.type === utils_1.AST_NODE_TYPES.Identifier &&
                    node.callee.object.name === "bindings" &&
                    node.callee.property.type === utils_1.AST_NODE_TYPES.Identifier &&
                    node.callee.property.name === "exit") {
                    if (node.parent?.type === utils_1.AST_NODE_TYPES.ExpressionStatement &&
                        node.parent.parent?.type === utils_1.AST_NODE_TYPES.Program) {
                        const arg = node.arguments[0];
                        if (arg.type === utils_1.AST_NODE_TYPES.CallExpression &&
                            arg.callee.type === utils_1.AST_NODE_TYPES.Identifier &&
                            arg.callee.name === "main") {
                            foundTopLevel = true;
                        }
                    }
                    else {
                        context.report({
                            node,
                            messageId: "notTopLevel",
                        });
                        hasReported = true;
                    }
                }
            },
            "Program:exit"(programNode) {
                if (!foundTopLevel && !hasReported) {
                    context.report({
                        node: programNode,
                        loc: {
                            line: programNode.loc.end.line,
                            column: 0,
                        },
                        messageId: "missingBindingsExitMain",
                    });
                    hasReported = true;
                }
            },
        };
    },
});
//# sourceMappingURL=enforce-bindings-exit-main.js.map