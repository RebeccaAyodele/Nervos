"use strict";
const index_js_1 = require("./rules/index.js");
const { name, version } = require("../package.json");
const plugin = {
    configs: {
        get recommended() {
            return recommended;
        },
    },
    meta: { name, version },
    rules: index_js_1.rules,
};
const recommended = {
    plugins: {
        'ckb-js-std': plugin,
    },
    rules: Object.keys(index_js_1.rules).reduce((acc, rule) => {
        acc[`ckb-js-std/${rule}`] = "error";
        return acc;
    }, {}),
};
module.exports = plugin;
//# sourceMappingURL=index.js.map